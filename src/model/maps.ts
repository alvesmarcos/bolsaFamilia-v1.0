import { Place } from './place';

export class Maps {
  constructor(
    id: string,
    staticMap: string,
    nis: string,
    cep: string,
    places: Place[],
    latitude: number,
    longitude: number
    ){}
}